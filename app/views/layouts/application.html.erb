<!DOCTYPE html>
<html lang="en">
  <head>
    <%= render "layouts/head" %>
    <%= render "layouts/pwa" %>
  </head>

  <body class="min-h-screen bg-paper text-ink antialiased">
    <%= render "shared/masthead" if authenticated? %>

    <main class="mx-auto w-full max-w-2xl px-5 pt-10 pb-safe">
      <%= render "shared/flash" %>
      <%= yield %>
    </main>
  </body>
</html>
